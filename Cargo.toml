[package]
name = "pokemon"
version = "0.1.0"
edition = "2024"

[profile.release]
lto = true
strip = true

[dependencies]
clap = { version = "4.5.39", features = ["derive", "help", "std", "usage"], default-features = false }
